import { MatchPasswordDirective } from './match-password-validation.directive';

describe('MatchPasswordValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
